# OKF Bundle Update Log

## 2026-06-28
* **Process**: Added [Deployment Perimeters](processes/deployment-perimeters.md) — CM5 LAN, hub OVH, armoire seule WAN, sites publics, jumeaux et commandes deploy.
* **Initialization**: Created the ESSENSYS Open Knowledge Format memory bundle with root index, directory indexes, foundational concepts, and OKF v0.1 reference.
## 2026-06-28
* **Update**: Applied `essensys-okf-discovery-2026-06-029`; generated repository, firmware, protocol, roadmap, portal, process and synthesis concepts.
* **Update**: Added ESSENSYS OKF Memory Sync skill/rule process so agents must refresh wiki/OKF when touching essensys-hub repositories.

## 2026-07-20
* **Update**: Applied OpenSpec `essensys-turnstile-registration-2026-07-036` — Turnstile on public registration (portal-backend + support-site SPA + Ansible templates). Secrets/SOPS values and OVH deploy remain operator steps.

## 2026-08-06
* **Protocol**: Added [Bus I2C BP ↔ BA](protocols/i2c-ba-bus.md) — topologie, transaction write+read, CRC-16/MODBUS, codes de trame et limites d'observabilité du bus interne armoire, sourcés du firmware SC944D et SC942C.
* **Proposal**: OpenSpec `essensys-i2c-ba-sniffer-2026-07-038` — sniffer I2C passif exploratoire (nouveau dépôt `essensys-i2c-ba-sniffer`), sans capacité d'émission.

## 2026-08-24
* **Deploy**: `essensys-password-reset-2026-08-039` tranche `admin-password-reset-assist` déployée sur OVH (`--tags cloud_backend`, commit `36ef04a`). Migration 013 appliquée : table `password_reset_tokens` créée, modèle `password_reset` réécrit autour de `{{reset_url}}` et activé. Routes `GET /api/auth/password/reset/validate` et `POST /api/auth/password/reset` vérifiées en production (400 `invalid_token` sur jeton bidon) ; `POST /api/admin/users/{id}/password-reset` monté et gardé (401 sans JWT admin). **Non fait** : SPA support-site (page `/reset-password`) toujours pas déployée, donc le lien émis n'a pas encore de page ; test d'envoi SMTP réel non exécuté faute de JWT admin.
* **Constat**: `www.essensys.fr` sert le build `essensys-support-site` et `mon.essensys.fr` le build `essensys-user-portal-frontend` — même VPS `37.59.106.164`, vhosts distincts. ~~`FRONTEND_URL=https://www.essensys.fr/` est donc correct pour `/reset-password`~~ — **affirmation fausse, corrigée le 2026-08-24 (voir entrée « Correction » ci-dessous)** : la valeur déployée est `https://mon.essensys.fr/`.
* **Proposal**: OpenSpec `essensys-password-reset-2026-08-039` — réinitialisation de mot de passe portail (jeton opaque à usage unique en base, lien `{{reset_url}}` remplaçant `{{temporary_password}}`, latence uniforme anti-énumération, extraction de `internal/mailtpl` hors du paquet `admin`, action admin tracée). Gap comblé : `internal/identity/routes.go` ne montait aucune route de reset et le modèle d'email `password_reset` (migration 006) n'était jamais envoyé.
* **Deploy**: `essensys-password-reset-2026-08-039` tranches `password-reset-request`, `password-reset-notification` et `auth-recovery-ui` déployées (backend `edc9892`, support-site `b96409f`). Le parcours est désormais atteignable : `POST /api/auth/password/forgot` (Turnstile obligatoire, 5 demandes/h par IP, 3 jetons/h par compte, `202` à corps constant quelle que soit l'existence du compte), lien « Mot de passe oublié ? » sur `/login` transmettant l'email saisi, page `/forgot-password`, et états de lien mort de `/reset-password` renvoyant vers une nouvelle demande. Rendu et envoi des modèles extraits dans `internal/mailtpl`, partagés par `admin` et `identity`. Vérifié en production : jeton émis pour `users.id=12`, `email_send_log` à `sent`, audit `PASSWORD_RESET_REQUESTED`, et l'émission d'un nouveau jeton invalide bien le précédent.
* **Correction**: `FRONTEND_URL` vaut `https://mon.essensys.fr/` sur OVH, pas `https://www.essensys.fr/` comme noté plus haut dans la journée. Or `/reset-password` n'existe que dans le build support-site : un lien construit sur `mon.essensys.fr` répond `200` par le repli SPA du portail puis n'affiche rien, panne visible seulement dans la boîte du destinataire. Le correctif précédent (`36ef04a`) ne touchait que le **repli**, jamais atteint puisque `FRONTEND_URL` est toujours défini. Le lien lit désormais `PASSWORD_RESET_BASE_URL` (`https://www.essensys.fr`, posé par `roles/cloud_backend/templates/cloud-backend.env.j2`) et n'utilise plus `FRONTEND_URL`, dont la valeur portail reste légitime pour `{{portal_url}}`. Régression épinglée par `TestResetLinkIgnoresPortalFrontendURL`.
* **Constat**: `notify.Render` laissait les marqueurs `{{...}}` non fournis dans le corps envoyé, ce qui imprimait par exemple `{{gateway_ip}}` dans un courriel client — panne visible uniquement dans la boîte du destinataire. Son propre test `TestRenderUnknownPlaceholderLeftEmpty` attendait déjà l'inverse et échouait depuis son écriture, rendant `go test ./...` durablement rouge. L'implémentation supprime désormais les marqueurs restants et la suite complète du dépôt passe.
